services:
  react:
    container_name: react
    build: 
      context: .
      dockerfile: dockerfile
    volumes:
      - ./app/react-app:/app
      # - /app/node_modules
    # command: sh -c "npm install && npm run start"
    command: npm run start
    ports:
      - 3000:3000
    stdin_open: true
    tty: true

  tests:
    container_name: react-test-puppeteer
    build:
      context: .
      dockerfile: dockerfile.puppeteer
    volumes:
      - ./test/puppeteer:/test
      - /test/node_modules
      - ./test/data:/data
    command: npm run start
    depends_on:
      - react
    stdin_open: true
    tty: true

  # tests:
  #   container_name: react-test-playwright
  #   build:
  #     context: .
  #     dockerfile: dockerfile.test
  #   volumes:
  #     - type: bind
  #       source: ./test
  #       target: /test
  #   command: pytest -s /test/test_playwright.py
  #   depends_on:
  #     - react
  #   stdin_open: true
  #   tty: true